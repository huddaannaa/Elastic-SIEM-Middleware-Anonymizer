# Elastic-SIEM-Middleware-Anonymizer
Privacy-preserving SIEM → ChatGPT middleware that anonymizes alerts, enforces a strict field allow-list, and adds API-key protection before sending data to OpenAI. Works with Elastic, Wazuh, and other SIEMs that can POST JSON
